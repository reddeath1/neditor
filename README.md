# neditor
Content management editor/ rich text editor

         /**
         * set default rendering data
         */
        n.configs({
            placeholder:dummy,
            defaultText:'create',
            hasTitle:false,
            canExpand:false,
            isClosable:false
        });
        
        /**
         * render data
         */
        n.render();
